<div class="min-h-screen pt-24 pb-12 flex flex-col items-center justify-center bg-gradient-to-b from-[#ffe2ea] via-[#ffe2ea] via-20% to-[#97DBC1]">
  
  <%# 画面全体のコンテナ。昨日のトップと同じグラデーションを背景に指定 %>
  <div class="w-full max-w-md px-4">

    <%# 白いカード（すりガラス風） %>
    <div class="bg-white p-8 md:p-10 rounded-3xl shadow-lg border border-none backdrop-blur-sm"
     style="background-color: rgba(255, 255, 255, 0.7);">
      
      <h2 class="text-3xl font-bold text-center text-gray-800 mb-10">ログイン</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      
        <%= render "devise/shared/flash_messages" %>
        
        <%# メールアドレス %>
        <div class="mb-6">
          <%= f.label :email, "メールアドレス", class: "block text-sm font-bold text-gray-600 mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "example@email.com",
              required: true,
              class: "w-full px-5 py-3 border border-gray-300 rounded-lg text-gray-800 placeholder:text-gray-300 focus:outline-none focus:ring-2 focus:ring-[#65C7A4]/50 focus:border-[#65C7A4] shadow-inner" %>
        </div>

        <%# パスワード %>
        <div class="mb-8">
          <%= f.label :password, "パスワード", class: "block text-sm font-bold text-gray-600 mb-2" %>
          <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワードを入力",
              required: true,
              class: "w-full px-5 py-3 border border-gray-300 rounded-lg text-gray-800 placeholder:text-gray-300 focus:outline-none focus:ring-2 focus:ring-[#65C7A4]/50 focus:border-[#65C7A4] shadow-inner" %>
        </div>

        <%# ログインするボタン %>
        <div class="text-center mb-6">
          <%= f.submit "ログインする", 
              class: "w-full md:w-auto px-10 py-3 text-white font-bold text-lg rounded-full transition-all hover:opacity-90 hover:shadow-lg shadow-md cursor-pointer",
              style: "background-color: #65C7A4;" %>
        </div>

      <% end %>

      <%# パスワードを忘れた場合（リンク） %>
      <div class="text-center mb-10">
        <%= link_to "パスワードを忘れた場合", new_password_path(resource_name), class: "text-sm text-[#F7A7A2] hover:text-[#f38d87] underline" %>
      </div>

      <%# または（ライン） %>
      <div class="relative flex items-center mb-10">
        <div class="flex-grow border-t border-gray-300"></div>
        <span class="flex-shrink mx-4 text-sm text-gray-500 font-medium">または</span>
        <div class="flex-grow border-t border-gray-300"></div>
      </div>

      <%# SNSログインボタン %>
      <div class="flex justify-center gap-x-6 mb-12">
        <%# Google %>
        <%= link_to "#", class: "w-14 h-14 rounded-2xl flex items-center justify-center bg-[#6CB69B] text-white font-bold text-xl shadow-md transition-transform hover:scale-105" do %>
          <span class="sr-only">Googleでログイン</span>
          G
        <% end %>
        
        <%# LINE %>
        <%= link_to "#", class: "w-14 h-14 rounded-2xl flex items-center justify-center bg-[#6CB69B] text-white font-bold text-xl shadow-md transition-transform hover:scale-105" do %>
          <span class="sr-only">LINEでログイン</span>
          LINE
        <% end %>

        <%# X (Twitter) %>
        <%= link_to "#", class: "w-14 h-14 rounded-2xl flex items-center justify-center bg-[#6CB69B] text-white font-bold text-xl shadow-md transition-transform hover:scale-105" do %>
          <span class="sr-only">Xでログイン</span>
          X
        <% end %>
      </div>

      <%# 境界線 %>
      <div class="border-t border-gray-300 mb-10"></div>

      <%# 初めての方はこちら %>
      <div class="text-center">
        <p class="text-base text-gray-700 font-bold mb-6">初めての方はこちら</p>
        <%= link_to "アカウント作成", new_registration_path(resource_name),
            class: "px-8 py-3 text-[#65C7A4] font-bold text-lg rounded-full border-2 border-[#65C7A4] inline-block transition-colors hover:bg-[#65C7A4]/10" %>
      </div>

    </div>
  </div>
</div>
